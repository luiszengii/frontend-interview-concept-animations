# 参与维护

欢迎补充更准确、更容易理解的前端概念动画。

提交前请确认：

1. 变化适合用动画表达，而不是纯定义题。
2. 内容包含真实实现边界，不只罗列 API。
3. 桌面端和移动端都能完成完整滚动流程。
4. 已运行 `npm run check`。
5. 已更新 `CHANGELOG.md`。

提交信息建议使用简短动词开头，例如：

```text
add promise concurrency scene
fix mobile card overflow
clarify SSE abort lifecycle
```
